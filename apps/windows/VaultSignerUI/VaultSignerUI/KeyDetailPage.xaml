<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="VaultSignerUI.KeyDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:VaultSignerUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer>
            <StackPanel Padding="32,24,32,32" Spacing="20" MaxWidth="480" HorizontalAlignment="Center">

                <StackPanel Spacing="4">
                    <HyperlinkButton x:Name="BackButtonElement" Padding="0" Click="BackButton_Click" />
                    <TextBlock x:Name="TitleText" FontSize="28" FontWeight="SemiBold" TextWrapping="Wrap" />
                </StackPanel>

                <InfoBar x:Name="StatusBar" IsOpen="False" Severity="Error" IsClosable="True" />

                <StackPanel Spacing="10" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" Padding="16" CornerRadius="6">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="DescriptionFieldText" FontWeight="SemiBold" Width="110" />
                        <TextBlock x:Name="DescriptionText" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="ResourceFieldText" FontWeight="SemiBold" Width="110" />
                        <TextBlock x:Name="ResourceText" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="TypePurposeFieldText" FontWeight="SemiBold" Width="110" />
                        <TextBlock x:Name="TypeText" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock x:Name="CreatedFieldText" FontWeight="SemiBold" Width="110" />
                        <TextBlock x:Name="CreatedText" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8" x:Name="LastUsedRow">
                        <TextBlock x:Name="LastUsedFieldText" FontWeight="SemiBold" Width="110" />
                        <TextBlock x:Name="LastUsedText" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8" x:Name="TagsRow">
                        <TextBlock x:Name="TagsFieldText" FontWeight="SemiBold" Width="110" />
                        <TextBlock x:Name="TagsText" TextWrapping="Wrap" />
                    </StackPanel>
                    <StackPanel Spacing="4">
                        <TextBlock x:Name="PublicKeyFieldText" FontWeight="SemiBold" />
                        <TextBlock x:Name="PublicKeyText" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" Opacity="0.85" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="ChangePassphraseButton" Click="ChangePassphraseButton_Click" />
                    <Button x:Name="ExportKeyButton" Click="ExportKeyButton_Click" />
                    <Button x:Name="RevealRawKeyButton" Foreground="{ThemeResource SystemFillColorCriticalBrush}" Click="RevealRawKeyButton_Click" />
                </StackPanel>

                <StackPanel Spacing="8" Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}" Padding="16" CornerRadius="6">
                    <TextBlock x:Name="DangerZoneText" FontWeight="SemiBold" Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                    <TextBlock x:Name="DiscardWarningText" TextWrapping="Wrap" Opacity="0.85" />
                    <TextBlock x:Name="ConfirmPrompt" TextWrapping="Wrap" FontWeight="SemiBold" Margin="0,4,0,0" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" x:Name="DiscardConfirmBox" />
                        <Button Grid.Column="1" x:Name="DiscardKeyButton" Margin="8,0,0,0" Click="DiscardKeyButton_Click" />
                    </Grid>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <ProgressRing x:Name="BusyRing" IsActive="False" Width="32" Height="32"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Page>
