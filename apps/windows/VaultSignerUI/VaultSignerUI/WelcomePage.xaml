<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="VaultSignerUI.WelcomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:VaultSignerUI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer>
            <StackPanel Padding="32,24,32,32" Spacing="20" MaxWidth="480" HorizontalAlignment="Center">

                <StackPanel Spacing="4">
                    <TextBlock Text="VaultSigner" FontSize="32" FontWeight="SemiBold" />
                    <TextBlock x:Name="SubtitleText" Opacity="0.7" TextWrapping="Wrap" />
                </StackPanel>

                <InfoBar x:Name="StatusBar" IsOpen="False" Severity="Error" IsClosable="True" />

                <StackPanel Spacing="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="RecentVaultsHeaderText" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <HyperlinkButton x:Name="ManageVaultsLinkButton" Grid.Column="1" Padding="0" Click="ManageVaultsLink_Click" />
                    </Grid>

                    <TextBlock x:Name="NoRecentVaultsText" Opacity="0.7" Visibility="Collapsed" />

                    <ListView x:Name="RecentVaultsList" SelectionMode="None" IsItemClickEnabled="True" ItemClick="RecentVaultsList_ItemClick" MaxHeight="220">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:KnownVaultRow">
                                <Grid Padding="4,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="{x:Bind FileName}" FontWeight="SemiBold"
                                                    Foreground="{x:Bind ForegroundBrush}" />
                                        <TextBlock Text="{x:Bind Path}" Opacity="0.6" FontSize="12" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                    <FontIcon Grid.Column="1" Glyph="&#xE7BA;" FontSize="14" Foreground="#F2A93B" Margin="8,0"
                                              Visibility="{x:Bind UnavailableVisibility}" ToolTipService.ToolTip="{x:Bind UnavailableTooltip}" />
                                    <Button Grid.Column="2" Background="Transparent" BorderThickness="0" Padding="6"
                                            Click="ForgetButton_Click" Tag="{x:Bind Path}" ToolTipService.ToolTip="{x:Bind ForgetTooltip}">
                                        <FontIcon Glyph="&#xE711;" FontSize="12" />
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>

                <StackPanel x:Name="CreateSection" Spacing="8">
                    <TextBlock x:Name="CreateSectionHeaderText" FontWeight="SemiBold" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Name="CreateFolderText" Opacity="0.7" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                        <Button x:Name="ChooseFolderButton" Grid.Column="1" Margin="8,0,0,0" Click="ChooseFolderButton_Click" />
                    </Grid>
                    <TextBox x:Name="NewVaultFileNameBox" Text="MyVault.vlt" />
                    <TextBox x:Name="CompartmentLabelBox" Text="Personal" />
                    <PasswordBox x:Name="MasterPassphraseBox" />
                    <PasswordBox x:Name="ConfirmPassphraseBox" />
                    <local:DeviceProfilePicker x:Name="ProfilePicker" />
                    <Button x:Name="CreateVaultButton" Style="{StaticResource AccentButtonStyle}"
                            HorizontalAlignment="Right" Click="CreateVaultButton_Click" />
                </StackPanel>

                <StackPanel Spacing="8">
                    <TextBlock x:Name="OpenSectionHeaderText" FontWeight="SemiBold" />
                    <Button x:Name="OpenVaultButton" Click="OpenVaultButton_Click" />
                </StackPanel>

            </StackPanel>
        </ScrollViewer>

        <ProgressRing x:Name="BusyRing" IsActive="False" Width="32" Height="32"
                      HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Grid>
</Page>
