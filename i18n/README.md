# VaultSigner — i18n

Single source-of-truth ICU MessageFormat resource files, plus per-platform
generation scripts (`.strings`, `strings.xml`, `.resx`, `.po`). Not started;
scaffolding begins in Phase 2 (spec §12, item 2.9).
