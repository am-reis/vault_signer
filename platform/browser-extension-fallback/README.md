# VaultSigner — Browser extension fallback

Shared WebExtension + native-messaging host, used as the Windows plugin-
authenticator fallback (spec §6.2) and as one of the two candidate Linux
FIDO2 approaches (spec §6.5). Not started; built only if/when a Phase 3 or
Phase 6 timebox requires it.
